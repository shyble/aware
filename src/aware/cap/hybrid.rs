// Hybrid cap variant: k-means bootstrap for initialization + gradient
// updates allowed + audit also fires. Provides both gradient-driven
// adaptation and lifecycle replacement. Most expressive but training
// dynamics need careful tuning.
