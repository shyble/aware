// Gradient-trained cap variant: keys initialized via Xavier random; both
// keys and (optional) values are gradient-updated like any other learnable
// parameter. No audit. This is structurally equivalent to a transformer
// FFN slot.
